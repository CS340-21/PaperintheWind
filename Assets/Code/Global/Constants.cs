using UnityEngine;

public class Constants
{

    // Where should the level begin generating?
    public static Vector3 WorldStart = new Vector3(100, 0, 100);

    // How many sections should be spawned at all times?
    public static int TotalSpawnedSections = 3;

}
